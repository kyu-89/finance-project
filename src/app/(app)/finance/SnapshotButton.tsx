'use client'; import { useActionState } from 'react'; import { saveSnapshotAction } from '@/actions/asset-actions'; import { FormMessage } from '@/components/FormMessage'; import { INITIAL_ACTION_STATE } from '@/lib/action-result';
export function SnapshotButton() { const [state, action, pending] = useActionState(saveSnapshotAction, INITIAL_ACTION_STATE); return <form action={action} className="finance-snapshot flex w-full max-w-xs flex-col items-stretch gap-1 md:w-auto md:items-end"><button disabled={pending} className="tds-primary-button w-full px-6 md:w-auto">{pending ? '기록하는 중...' : '자산 업데이트'}</button><FormMessage result={state} successMessage="이번 달 자산 현황을 기록했어요." /></form>; }
