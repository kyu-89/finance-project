'use client'; import { useActionState } from 'react'; import { saveSnapshotAction } from '@/actions/asset-actions'; import { FormMessage } from '@/components/FormMessage'; import { INITIAL_ACTION_STATE } from '@/lib/action-result';
export function SnapshotButton() { const [state, action, pending] = useActionState(saveSnapshotAction, INITIAL_ACTION_STATE); return <form action={action} className="flex max-w-xs flex-col items-end gap-1"><button disabled={pending} className="tds-primary-button px-6">{pending ? '기록하는 중...' : '이번 달 자산 현황 기록하기'}</button><p className="text-right text-xs leading-5 text-[var(--tds-grey-500)]">오늘 기준 금액을 남겨 다음 달 변화와 비교해요.<br />이번 달 기록이 있으면 최신 금액으로 바뀌어요.</p><FormMessage result={state} successMessage="이번 달 자산 현황을 기록했어요." /></form>; }
