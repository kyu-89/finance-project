'use client';
import { useActionState } from 'react'; import { createAssetAction, disposeAssetAction, updateAssetValueAction } from '@/actions/asset-actions'; import { FormMessage } from '@/components/FormMessage'; import { INITIAL_ACTION_STATE } from '@/lib/action-result'; import type { Asset } from '@/lib/assets'; import type { HouseholdMember } from '@/lib/household';
const won = new Intl.NumberFormat('ko-KR'); const typeName = { real_estate: '부동산', car: '자동차', precious_metal: '귀금속', other: '기타' };
export function AssetManager({ assets, members, today }: { assets: Asset[]; members: HouseholdMember[]; today: string }) { const [state, action, pending] = useActionState(createAssetAction, INITIAL_ACTION_STATE); return <div className="flex flex-col gap-5"><form action={action} className="tds-card grid gap-4 p-5 md:grid-cols-2 xl:grid-cols-4"><div className="md:col-span-2 xl:col-span-4"><FormMessage result={state} /></div><Field label="자산명"><input name="assetName" required className="px-3" /></Field><Field label="유형"><select name="assetType" className="px-3"><option value="real_estate">부동산</option><option value="car">자동차</option><option value="precious_metal">귀금속</option><option value="other">기타</option></select></Field><Field label="취득가"><input name="acquisitionCost" type="number" min="0" step="1" defaultValue="0" required className="px-3 text-right" /></Field><Field label="현재 평가액"><input name="currentValue" type="number" min="0" step="1" required className="px-3 text-right" /></Field><Field label="평가기준일"><input name="valuationDate" type="date" defaultValue={today} required className="px-3" /></Field><Field label="명의자"><select name="ownerMemberId" className="px-3"><option value="">지정 안 함</option>{members.map((m) => <option key={m.id} value={m.id}>{m.displayName}</option>)}</select></Field><Field label="비고"><input name="memo" className="px-3" /></Field><button disabled={pending} className="tds-primary-button md:col-span-2 xl:col-span-4">{pending ? '저장 중...' : '자산 추가'}</button></form><div className="grid gap-4 md:grid-cols-2">{assets.length === 0 && <p className="tds-card p-6 text-sm text-[var(--tds-grey-500)]">등록한 기타자산이 없어요.</p>}{assets.map((a) => <AssetCard key={a.id} asset={a} />)}</div></div>; }
function Field({ label, children }: { label: string; children: React.ReactNode }) { return <label className="flex flex-col gap-1 text-sm font-medium">{label}{children}</label>; }
function AssetCard({ asset }: { asset: Asset }) { const [valueState, valueAction, valuePending] = useActionState(updateAssetValueAction, INITIAL_ACTION_STATE); const [disposeState, disposeAction, disposePending] = useActionState(disposeAssetAction, INITIAL_ACTION_STATE); const active = asset.status === 'active'; return <article className={`tds-card flex flex-col gap-4 p-5 ${active ? '' : 'opacity-65'}`}><div className="flex justify-between gap-3"><div><h2 className="font-bold">{asset.assetName}</h2><p className="text-sm text-[var(--tds-grey-700)]">{typeName[asset.assetType]} · {asset.valuationDate} 기준</p></div><span className="rounded-full bg-[var(--tds-grey-100)] px-2 py-1 text-xs">{active ? '보유 중' : '처분'}</span></div><div><p className="text-xs text-[var(--tds-grey-500)]">현재 평가액</p><strong className="text-xl">{won.format(asset.currentValue)}원</strong><p className="text-xs text-[var(--tds-grey-500)]">취득가 {won.format(asset.acquisitionCost)}원</p></div>{active && <><form action={valueAction} className="flex gap-2"><input type="hidden" name="id" value={asset.id} /><input name="value" type="number" min="0" step="1" defaultValue={asset.currentValue} className="min-w-0 flex-1 px-3 text-right" /><button disabled={valuePending} className="secondary-button px-4">평가액 수정</button></form><FormMessage result={valueState} /><form action={disposeAction}><input type="hidden" name="id" value={asset.id} /><button disabled={disposePending} className="secondary-button w-full text-[var(--tds-red-500)]">처분 처리</button></form><FormMessage result={disposeState} /></>}</article>; }
