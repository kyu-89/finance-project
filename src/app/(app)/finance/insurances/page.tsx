import Link from 'next/link'; import { ensureHouseholdForCurrentUser, listHouseholdMembers } from '@/lib/household'; import { listInsurances } from '@/lib/insurances'; import { listPaymentMethods } from '@/lib/payment-methods'; import { InsuranceManager } from './InsuranceManager';
export default async function InsurancesPage() { const household = await ensureHouseholdForCurrentUser(); const [insurances, members, methods] = await Promise.all([listInsurances(household.id), listHouseholdMembers(household.id), listPaymentMethods(household.id)]); return <div className="tds-page flex flex-col gap-6"><div><Link href="/finance" className="text-sm font-semibold text-[var(--tds-blue-500)]">← 자산·금융 전체</Link><h1 className="tds-title mt-3">보험을 관리해요</h1></div><InsuranceManager insurances={insurances} members={members} paymentMethods={methods} /></div>; }
