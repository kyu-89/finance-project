import Link from 'next/link';
const won = new Intl.NumberFormat('ko-KR');
const money = (value: number) => `${won.format(Math.round(value))}원`;
export function DashboardDebtOverview({ totalDebt, debtRatio, principal, financeCost }: { totalDebt: number; debtRatio: number; principal: number; financeCost: number }) { return <div className="home-html-asset-view"><div className="home-html-kpi-grid"><Kpi label="대출 잔금" value={money(totalDebt)} meta="현재 남은 부채" /><Kpi label="LTV" value={`${(debtRatio * 100).toFixed(1)}%`} meta="총자산 대비 부채" /><Kpi label="이번 달 원금" value={money(principal)} meta="확정 상환액" /><Kpi label="이번 달 이자" value={money(financeCost)} meta="확정 금융비용" /></div><section className="tds-card home-html-card"><div className="home-html-title"><h2>대출 현황</h2><span>잔금·상환 흐름은 대출 관리에서 관리</span></div><div className="home-debt-track"><span style={{ width: `${Math.min(100, debtRatio * 100)}%` }} /></div><p className="home-html-note">총자산 대비 부채 비율은 {(debtRatio * 100).toFixed(1)}%입니다. 원금 상환과 조기상환 기록은 대출 관리에서 확인할 수 있습니다.</p><Link href="/finance/loans" prefetch className="home-html-link">대출 관리 →</Link></section></div>; }
function Kpi({ label, value, meta }: { label: string; value: string; meta: string }) { return <article className="tds-card home-html-kpi"><span>{label}</span><strong>{value}</strong><small>{meta}</small></article>; }
