'use client';
import * as XLSX from 'xlsx';
import { useActionState, useMemo, useState } from 'react';
import { importTransactionsAction } from '@/actions/import-actions';
import { FormMessage } from '@/components/FormMessage';
import { INITIAL_ACTION_STATE } from '@/lib/action-result';
import { findHeaderRow, mapImportRows, type ParsedImportRow } from '@/lib/transaction-import';
import type { Category } from '@/lib/categories';
import type { PaymentMethod } from '@/lib/payment-methods';
export function WorkbookMonthlyImport({ categories, paymentMethods }: { categories: Category[]; paymentMethods: PaymentMethod[] }) { const [rows, setRows] = useState<ParsedImportRow[]>([]); const [fileName, setFileName] = useState(''); const [paymentMethodId, setPaymentMethodId] = useState(paymentMethods[0]?.id ?? ''); const [fallbackCategoryId, setFallbackCategoryId] = useState(''); const [message, setMessage] = useState(''); const [state, action, pending] = useActionState(importTransactionsAction, INITIAL_ACTION_STATE); const categoryByName = useMemo(() => new Map(categories.map((category) => [category.name.toLocaleLowerCase(), category.id])), [categories]); const validRows = useMemo(() => rows.filter((row) => row.errors.length === 0 && row.transactionDate && row.amount).map((row) => ({ transactionDate: row.transactionDate as string, transactionType: row.transactionType, amount: row.amount as number, description: row.description, categoryId: row.categoryName ? categoryByName.get(row.categoryName.toLocaleLowerCase()) ?? fallbackCategoryId ?? null : fallbackCategoryId || null, paymentMethodId, memo: row.memo ?? `Excel 월별 가져오기: ${fileName}`, needsReview: !row.categoryName || !categoryByName.has(row.categoryName.toLocaleLowerCase()) })), [rows, categoryByName, fallbackCategoryId, paymentMethodId, fileName]); async function handleFile(file: File) { setFileName(file.name); setRows([]); setMessage(''); try { const workbook = XLSX.read(await file.arrayBuffer(), { type: 'array', cellDates: true, dense: true }); const monthly = workbook.SheetNames.filter((name) => /^(?:[1-9]|1[0-2])월$/.test(name)); const parsed = monthly.flatMap((name) => { const sheetRows = XLSX.utils.sheet_to_json(workbook.Sheets[name], { header: 1, raw: true, defval: '' }) as unknown[][]; const found = findHeaderRow(sheetRows); return found ? mapImportRows(sheetRows, found.headers, found.mapping, found.rowIndex) : []; }); if (!parsed.length) setMessage('1월~12월 시트에서 거래 표를 찾지 못했습니다.'); setRows(parsed); } catch { setMessage('Excel 파일을 읽지 못했습니다.'); } } return <section className="tds-card p-5"><div className="flex flex-wrap items-start justify-between gap-3"><div><h2 className="text-lg font-bold">월별 거래 전체 가져오기</h2><p className="mt-1 text-sm text-[var(--tds-grey-700)]">1월~12월 시트를 한 번에 읽어 거래 원장에 추가합니다.</p></div><label className="tds-button-secondary inline-flex cursor-pointer px-4"><span>Excel 파일 선택</span><input className="sr-only" type="file" accept=".xlsx,.xls,.xlsm" onChange={(event) => { const file = event.target.files?.[0]; if (file) void handleFile(file); }} /></label></div>{message && <p role="alert" className="mt-3 text-sm text-[var(--tds-red-500)]">{message}</p>}{rows.length > 0 && <><div className="mt-4 grid gap-3 md:grid-cols-2"><label className="flex flex-col gap-1 text-sm font-medium">기본 결제수단<select className="tds-input" value={paymentMethodId} onChange={(event) => setPaymentMethodId(event.target.value)}><option value="">선택해 주세요</option>{paymentMethods.map((method) => <option key={method.id} value={method.id}>{method.name}</option>)}</select></label><label className="flex flex-col gap-1 text-sm font-medium">분류를 못 찾은 행의 기본 카테고리<select className="tds-input" value={fallbackCategoryId} onChange={(event) => setFallbackCategoryId(event.target.value)}><option value="">미분류로 저장</option>{categories.map((category) => <option key={category.id} value={category.id}>{category.name}</option>)}</select></label></div><p className="mt-4 text-sm">총 {rows.length}건 · 가져오기 가능 {validRows.length}건 · 오류 {rows.length - validRows.length}건</p><FormMessage result={state} /><form action={action}><input type="hidden" name="rows" value={JSON.stringify(validRows)} /><button disabled={pending || !paymentMethodId || !validRows.length} className="tds-primary-button mt-4 w-full">{pending ? '가져오는 중…' : `${validRows.length}건 가져오기`}</button></form></>}</section>; }
