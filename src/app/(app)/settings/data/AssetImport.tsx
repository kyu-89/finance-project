'use client';
import * as XLSX from 'xlsx'; import { useActionState, useMemo, useState } from 'react'; import { importAssetsAction } from '@/actions/asset-actions'; import { FormMessage } from '@/components/FormMessage'; import { INITIAL_ACTION_STATE } from '@/lib/action-result'; import { parseAssetRows, type ParsedAsset } from '@/lib/excel-asset-import';
export function AssetImport() { const [assets, setAssets] = useState<ParsedAsset[]>([]); const [message, setMessage] = useState(''); const [assetState, assetAction, assetPending] = useActionState(importAssetsAction, INITIAL_ACTION_STATE); const validAssets = useMemo(() => assets.filter((row) => row.errors.length === 0 && row.currentValue !== null), [assets]); async function handleFile(file: File) { setAssets([]); setMessage(''); try { const workbook = XLSX.read(await file.arrayBuffer(), { type: 'array', cellDates: true, dense: true }); const sheet = (name: string) => workbook.Sheets[workbook.SheetNames.find((item) => item === name) ?? '']; const assetSheet = sheet('자산현황'); if (assetSheet) setAssets(parseAssetRows(XLSX.utils.sheet_to_json(assetSheet, { header: 1, raw: true, defval: '' }) as unknown[][])); if (!assetSheet) setMessage('자산현황 시트를 찾지 못했습니다.'); } catch { setMessage('Excel 파일을 읽지 못했습니다.'); } } return <section className="tds-card p-5"><div className="flex flex-wrap items-start justify-between gap-3"><div><h2 className="text-lg font-bold">자산 Excel 가져오기</h2><p className="mt-1 text-sm text-[var(--tds-grey-700)]">자산현황을 확인 후 등록합니다.</p></div><label className="tds-button-secondary inline-flex cursor-pointer px-4"><span>Excel 파일 선택</span><input className="sr-only" type="file" accept=".xlsx,.xls,.xlsm" onChange={(event) => { const file = event.target.files?.[0]; if (file) void handleFile(file); }} /></label></div>{message && <p role="alert" className="mt-3 text-sm text-[var(--tds-red-500)]">{message}</p>}{assets.length > 0 && <><p className="mt-4 text-sm">자산 {validAssets.length}/{assets.length}건 등록 가능</p><FormMessage result={assetState} /><form action={assetAction}><input type="hidden" name="assets" value={JSON.stringify(validAssets)} /><button disabled={assetPending || !validAssets.length} className="tds-primary-button mt-3 w-full">{assetPending ? '자산 가져오는 중…' : `${validAssets.length}건 자산 가져오기`}</button></form></>}</section>; }
