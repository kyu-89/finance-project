'use client';
import * as XLSX from 'xlsx';
import { useActionState, useMemo, useState } from 'react';
import { importInsurancesAction } from '@/actions/finance-product-actions';
import { FormMessage } from '@/components/FormMessage';
import { INITIAL_ACTION_STATE } from '@/lib/action-result';
import { parseInsuranceRows, type ParsedInsurance } from '@/lib/excel-loan-insurance-import';
export function InsuranceImport() { const [rows, setRows] = useState<ParsedInsurance[]>([]); const [fileName, setFileName] = useState(''); const [message, setMessage] = useState(''); const [state, action, pending] = useActionState(importInsurancesAction, INITIAL_ACTION_STATE); const valid = useMemo(() => rows.filter((row) => row.errors.length === 0), [rows]); async function handleFile(file: File) { setFileName(file.name); setRows([]); setMessage(''); try { const workbook = XLSX.read(await file.arrayBuffer(), { type: 'array', cellDates: true, dense: true }); const sheet = workbook.Sheets[workbook.SheetNames.find((name) => name === '보험') ?? '']; if (!sheet) { setMessage('보험 시트를 찾지 못했습니다.'); return; } setRows(parseInsuranceRows(XLSX.utils.sheet_to_json(sheet, { header: 1, raw: true, defval: '' }) as unknown[][])); } catch { setMessage('Excel 파일을 읽지 못했습니다.'); } } return <section className="tds-card p-5"><div className="flex flex-wrap items-start justify-between gap-3"><div><h2 className="text-lg font-bold">보험 Excel 가져오기</h2><p className="mt-1 text-sm text-[var(--tds-grey-700)]">보험 시트의 보장·납부·만료 정보를 확인 후 등록합니다.</p></div><label className="tds-button-secondary inline-flex cursor-pointer px-4"><span>Excel 파일 선택</span><input className="sr-only" type="file" accept=".xlsx,.xls,.xlsm" onChange={(event) => { const file = event.target.files?.[0]; if (file) void handleFile(file); }} /></label></div>{fileName && <p className="mt-3 text-sm">선택 파일: <strong>{fileName}</strong></p>}{message && <p role="alert" className="mt-3 text-sm text-[var(--tds-red-500)]">{message}</p>}{rows.length > 0 && <><p className="mt-4 text-sm">총 {rows.length}건 · 등록 가능 {valid.length}건 · 오류 {rows.length - valid.length}건</p><div className="mt-3 overflow-x-auto"><table className="w-full min-w-[700px] text-left text-sm"><thead><tr className="border-b text-xs text-[var(--tds-grey-500)]"><th className="py-2">피보험자</th><th>보험사</th><th>보험명</th><th>가입일</th><th className="text-right">월보험료</th><th>상태</th></tr></thead><tbody>{rows.slice(0, 30).map((row) => <tr key={`${row.rowNumber}-${row.productName}`} className="border-b border-[var(--tds-grey-100)]"><td className="py-2">{row.insuredMember || '-'}</td><td>{row.insurerName}</td><td>{row.productName}</td><td>{row.joinedAt ?? '-'}</td><td className="text-right tabular-nums">{row.monthlyPremium === null ? '-' : `${row.monthlyPremium.toLocaleString('ko-KR')}원`}</td><td className={row.errors.length ? 'text-[var(--tds-red-500)]' : 'text-[var(--tds-green-600)]'}>{row.errors.length ? row.errors.join(' ') : '가져오기 가능'}</td></tr>)}</tbody></table></div><FormMessage result={state} /><form action={action}><input type="hidden" name="insurances" value={JSON.stringify(valid)} /><button disabled={pending || valid.length === 0} className="tds-primary-button mt-4 w-full">{pending ? '가져오는 중…' : `${valid.length}건 가져오기`}</button></form></>}</section>; }
