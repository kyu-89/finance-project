import { PlanningSettingsPage } from '../PlanningSettingsPage';

export default function PlanningSettingsRoute() {
  return <PlanningSettingsPage title="재무 목표·일정" description="가족의 재무 목표와 중요한 일정을 함께 관리해요." />;
}
